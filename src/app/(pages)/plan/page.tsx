import { PlanCard } from '@/components/organisms/PlanCard';
import type { NextPage } from 'next/types';

const Plan: NextPage = () => {
  return (
    <>
      <PlanCard variant='detail' imagePath='' heading={<></>} content={<></>} />
    </>
  );
};

export default Plan;
